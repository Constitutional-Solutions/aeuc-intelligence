# Repository Layout

This repository distinguishes between core, research, and experiments.

- `src/core/` – Production, trusted logic.
- `src/research/` – Experimental modules and hypotheses.
- `experiments/` – One-off notebooks, scripts, and prototypes.

This file exists to ensure the directories are tracked by Git; it can also be
extended with per-directory README content over time.
